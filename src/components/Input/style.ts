import styled from "styled-components";
import { IInputProps } from '../../types/InputProps';

export const ProductSearchInput = styled.input<IInputProps>`
  width: 30vw;
  height: 5vh;
  margin-top: 20px;
`;